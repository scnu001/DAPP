# 钱包模块的配置项 —— 全是「可选」的。
#
# ⚠️ 重点：连接钱包是**纯前端 + 钱包扩展**的行为，不需要任何后端、密钥或 AppID。
#    这里唯一可能用到的只有一个只读 RPC（给将来的只读组件用），本模块自身不用它。
#
# 复制成 .env 才生效（Vite 只读取 VITE_ 前缀的变量）。

# 只读节点（免 key）。不配就用 lib/chain.js 里内置的公共节点。
VITE_SEPOLIA_RPC_URL=https://ethereum-sepolia-rpc.publicnode.com
