# ============================================================
#  本地开发目录不入库
# ------------------------------------------------------------
#  注意：下面这行会同时排除根目录 e2e/ 和 wallet-login/e2e/
#  （gitignore 的 "e2e/" 不带前导斜杠时匹配任意层级的同名目录）。
# ============================================================
e2e/

# ============================================================
#  私钥 / 环境变量
# ============================================================
# 根目录 .env 里有 DEPLOYER_PRIVATE_KEY / TEST_CLAIMER_PRIVATE_KEY，绝不入库
.env
.env.local
.env.*.local

# 但示例文件必须进库（同事据此创建自己的 .env）
!.env.example
!**/.env.example

# ============================================================
#  依赖与编译产物
# ============================================================
node_modules/
dist/
build/
artifacts/
cache/
coverage/
typechain-types/
.vite/
*.tsbuildinfo

# ============================================================
#  Hardhat 本地链部署产物（只对本地有意义；sepolia.json 要保留入库）
# ============================================================
deployments/hardhat.json
deployments/localhost.json

# ============================================================
#  浏览器 profile / 截图等本地运行产物
#  ★ 每个 .edge-* profile 100~160MB，且内容是大段随机二进制，
#    一旦提交会把仓库彻底拖垮，这里做一层兜底。
# ============================================================
.edge-*/
**/.edge-*/
shots-*/
**/shots-*/
.*-profile.txt
**/.*-profile.txt

# ============================================================
#  日志
# ============================================================
*.log
npm-debug.log*
yarn-error.log*
pnpm-debug.log*

# ============================================================
#  编辑器 / 操作系统
# ============================================================
.vscode/*
!.vscode/extensions.json
!.vscode/settings.json
.idea/
*.swp
*.swo
*~
.DS_Store
Thumbs.db
desktop.ini
